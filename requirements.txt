# ───────────────────────────── requirements.txt ─────────────────────────────
# Core LLM client
openai>=1.14,<2.0          # modern SDK with httpx built‑in

# NLP sentence splitter
spacy>=3.7,<4.0            # small‑model compatible

# Progress‑bar utility (used in preprocess_csv.py)
tqdm>=4.66,<5.0

# Read .env values for your OpenAI key (llm_utils relies on it)
python-dotenv>=1.0,<2.0

# LangGraph pipeline + optional diagram rendering
langgraph>=0.2.34,<0.3
typing-extensions>=4.10,<5.0
ipython>=8.20,<9.0        # only used if you display() things in notebooks

rapidfuzz>=3.9,<4.0       # fast, lightweight string similarity
pandas>=2.2,<3.0          # used by the Streamlit dashboard
streamlit>=1.36,<2.0      # lightweight dashboard

